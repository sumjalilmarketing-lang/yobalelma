"use client";

import Link from "next/link";
import { useState, type FormEvent } from "react";
import { Activity, AlertTriangle, BarChart3, Boxes, Building2, Download, FileText, Gauge, Search, ShieldCheck, Users } from "lucide-react";
import { useLocalization } from "@/components/i18n/localization-provider";
import type { SupportedLocale } from "@/lib/i18n/config";
import type { EnterpriseHubState } from "../lib/enterprise-types";
import type { HubSession } from "../lib/types";
import { Badge, PageHeader, Panel, inputClass, selectClass, submitClass } from "./hub-ui";

type EnterprisePageKind = "controlTower" | "agents" | "search" | "incidents" | "alerts" | "stockMonitoring" | "forecast" | "systemHealth" | "audit" | "reports" | "exports" | "documents";

const fr = {
  controlTower: "Control Tower", controlTowerSub: "Supervision consolidée des hubs, volumes, capacités et risques opérationnels.", executive: "Vue exécutive", operations: "Vue opérationnelle", allHubs: "Tous les hubs", updated: "Actualisé", hub: "Hub", status: "Statut", inbound: "Entrants", inventory: "En stock", batches: "Lots actifs", incidents: "Incidents", capacity: "Capacité", alerts: "Alertes", comparison: "Comparaison des hubs", networkMap: "Carte du réseau", agents: "Performance des agents", agentsSub: "Indicateurs assistifs par agent, équipe et poste. Ils ne déclenchent aucune décision disciplinaire automatique.", agent: "Agent", team: "Équipe", station: "Poste", received: "Réceptions", inspections: "Inspections", movements: "Mouvements", weight: "Poids traité", average: "Temps moyen", sla: "SLA", productivity: "Productivité", search: "Recherche globale", searchSub: "Retrouvez instantanément colis, manifestes, lots, incidents et emplacements dans votre périmètre autorisé.", searchPlaceholder: "Tracking, manifeste, lot, incident…", searchHelp: "Saisissez au moins deux caractères. Raccourci : Ctrl/⌘ + K.", searchAction: "Rechercher", incidentsTitle: "Centre d’incidents", incidentsSub: "Priorisation, assignation, escalade, suivi SLA, résolution et post-mortem.", createIncident: "Créer un incident", code: "Référence", priority: "Priorité", assigned: "Assigné à", opened: "Ouvert", slaRemaining: "SLA", alertsTitle: "Alertes temps réel", alertsSub: "Alertes opérationnelles in-app avec canaux externes activables par configuration.", severity: "Sévérité", message: "Message", triggered: "Déclenchée", stock: "Supervision du stock", stockSub: "Occupation, ancienneté, stock dormant, saturation et rapprochement physique/système.", occupation: "Occupation", dormant: "Stock dormant", noLocation: "Sans emplacement", discrepancy: "Écarts", physicalCount: "Inventaire tournant", forecast: "Prévision et planification", forecastSub: "Recommandations assistives sur les volumes, capacités et ressources. Aucune action irréversible n’est automatique.", date: "Date", expected: "Colis attendus", expectedWeight: "Poids attendu", travelerCapacity: "Capacité voyageurs", agentsNeeded: "Agents requis", locationsNeeded: "Emplacements", saturationRisk: "Risque saturation", delayRisk: "Risque retard", health: "Santé du système", healthSub: "Disponibilité et latence des dépendances critiques du Hub.", service: "Service", latency: "Latence", availability: "Disponibilité", version: "Version déployée", audit: "Journal d’audit", auditSub: "Événements critiques append-only, corrélés et limités aux hubs autorisés.", actor: "Utilisateur", role: "Rôle", action: "Action", resource: "Ressource", result: "Résultat", risk: "Risque", correlation: "Corrélation", reports: "Rapports et analytics", legacyReports: "Rapports", reportsSub: "Tendances opérationnelles et exécutives par hub, destination et période.", growth: "Croissance", quality: "Qualité", reportsTrend: "Tendance sur 7 jours", exports: "Rapports et exports", exportsSub: "Exports horodatés et auditables, minimisés selon les permissions de l’auteur.", exportType: "Données", format: "Format", period: "Période", generate: "Générer", daily: "Journalier", weekly: "Hebdomadaire", monthly: "Mensuel", inventoryExport: "Inventaire", incidentsExport: "Incidents", auditExport: "Audit", performanceExport: "Performance", documents: "Documents professionnels", documentsSub: "Modèles A4 et étiquettes prêts à imprimer, avec références, QR et signatures.", print: "Imprimer", manifest: "Manifeste de réception", packingList: "Packing list", batchSheet: "Fiche lot", inspectionSheet: "Fiche d’inspection", inventorySheet: "Inventaire", sourceLive: "Données temps réel", sourceDemo: "Mode démonstration sécurisé", operational: "Opérationnel", degraded: "Dégradé", open: "Ouvert", acknowledged: "Pris en charge", resolved: "Résolu", critical: "Critique", high: "Élevée", warning: "Avertissement", info: "Information",
};
type Copy = typeof fr;
const en: Copy = { ...fr, controlTowerSub: "Consolidated supervision of hubs, volumes, capacity and operational risk.", executive: "Executive view", operations: "Operations view", allHubs: "All hubs", updated: "Updated", hub: "Hub", status: "Status", inbound: "Inbound", inventory: "In stock", batches: "Active batches", incidents: "Incidents", capacity: "Capacity", alerts: "Alerts", comparison: "Hub comparison", networkMap: "Network map", agents: "Agent performance", agentsSub: "Assistive metrics by agent, team and station. They never trigger automated disciplinary decisions.", agent: "Agent", team: "Team", station: "Station", received: "Received", inspections: "Inspections", movements: "Movements", weight: "Processed weight", average: "Average time", sla: "SLA", productivity: "Productivity", search: "Global search", searchSub: "Instantly find shipments, manifests, batches, incidents and locations within your authorized scope.", searchPlaceholder: "Tracking, manifest, batch, incident…", searchHelp: "Enter at least two characters. Shortcut: Ctrl/⌘ + K.", searchAction: "Search", incidentsTitle: "Incident center", incidentsSub: "Prioritization, assignment, escalation, SLA tracking, resolution and post-mortem.", createIncident: "Create incident", code: "Reference", priority: "Priority", assigned: "Assigned to", opened: "Opened", slaRemaining: "SLA", alertsTitle: "Real-time alerts", alertsSub: "In-app operational alerts with configurable external channels.", severity: "Severity", message: "Message", triggered: "Triggered", stock: "Stock supervision", stockSub: "Occupancy, ageing, dormant stock, saturation and physical/system reconciliation.", occupation: "Occupancy", dormant: "Dormant stock", noLocation: "No location", discrepancy: "Discrepancies", physicalCount: "Cycle count", forecast: "Forecast and planning", forecastSub: "Assistive recommendations for volumes, capacity and resources. No irreversible action is automated.", date: "Date", expected: "Expected parcels", expectedWeight: "Expected weight", travelerCapacity: "Traveler capacity", agentsNeeded: "Required agents", locationsNeeded: "Locations", saturationRisk: "Saturation risk", delayRisk: "Delay risk", health: "System health", healthSub: "Availability and latency of critical Hub dependencies.", service: "Service", latency: "Latency", availability: "Availability", version: "Deployed version", audit: "Audit log", auditSub: "Append-only, correlated critical events restricted to authorized hubs.", actor: "User", role: "Role", action: "Action", resource: "Resource", result: "Result", risk: "Risk", correlation: "Correlation", reports: "Reports and analytics", reportsSub: "Operational and executive trends by hub, destination and period.", growth: "Growth", quality: "Quality", reportsTrend: "7-day trend", exports: "Reports and exports", exportsSub: "Timestamped, auditable exports minimized according to author permissions.", exportType: "Data", format: "Format", period: "Period", generate: "Generate", daily: "Daily", weekly: "Weekly", monthly: "Monthly", inventoryExport: "Inventory", incidentsExport: "Incidents", auditExport: "Audit", performanceExport: "Performance", documents: "Professional documents", documentsSub: "Print-ready A4 and label templates with references, QR and signatures.", print: "Print", manifest: "Inbound manifest", packingList: "Packing list", batchSheet: "Batch sheet", inspectionSheet: "Inspection sheet", inventorySheet: "Inventory", sourceLive: "Real-time data", sourceDemo: "Secure demonstration mode", operational: "Operational", degraded: "Degraded", open: "Open", acknowledged: "Acknowledged", resolved: "Resolved", critical: "Critical", high: "High", warning: "Warning", info: "Information" };
const copies: Record<SupportedLocale, Copy> = { fr, en, es: en, pt: en, de: en, it: en, nl: en, ar: en, ru: en, "zh-CN": en };

export function EnterprisePage({ kind, state, session }: { kind: EnterprisePageKind; state: EnterpriseHubState; session: HubSession }) {
  const { formatDate, settings } = useLocalization();
  const t = copies[settings.locale] ?? fr;
  const source = state.source === "supabase" ? t.operations : t.executive;
  if (kind === "controlTower") return <ControlTower state={state} t={t} source={source} formatDate={formatDate} />;
  if (kind === "agents") return <Agents state={state} t={t} />;
  if (kind === "search") return <SearchPage state={state} t={t} />;
  if (kind === "incidents") return <Incidents state={state} t={t} />;
  if (kind === "alerts") return <Alerts state={state} t={t} formatDate={formatDate} />;
  if (kind === "stockMonitoring") return <Stock state={state} t={t} />;
  if (kind === "forecast") return <Forecast state={state} t={t} />;
  if (kind === "systemHealth") return <SystemHealth state={state} t={t} />;
  if (kind === "audit") return <Audit state={state} t={t} formatDate={formatDate} />;
  if (kind === "reports") return <Reports state={state} t={t} />;
  if (kind === "exports") return <Exports state={state} session={session} t={t} />;
  return <Documents t={t} />;
}

export function EnterpriseDashboard({ state }: { state: EnterpriseHubState }) {
  const { settings } = useLocalization();
  const t = copies[settings.locale] ?? fr;
  const hub = state.hubs.find((item) => item.id === state.activeHubId) ?? state.hubs[0];
  if (!hub) return null;
  const metrics = [
    [t.inbound, hub.inbound], [t.received, hub.inventory], [t.incidents, hub.incidents], [t.alerts, hub.criticalAlerts],
    [t.batches, hub.batches], [t.weight, `${hub.storedWeightKg} kg`], [t.capacity, `${Math.round(hub.reservedCapacityKg / Math.max(hub.travelerCapacityKg, 1) * 100)}%`],
    [t.inspections, Math.round(hub.inventory * 0.62)], [t.dormant, Math.round(hub.inventory * 0.04)], [t.noLocation, Math.round(hub.inventory * 0.015)],
    [t.sla, "94%"], [t.average, "18 min"], [t.productivity, "87%"], [t.saturationRisk, `${Math.round(hub.storedWeightKg / Math.max(hub.storageCapacityKg, 1) * 100)}%`],
  ];
  return <section className="space-y-3" data-testid="enterprise-dashboard"><div className="flex flex-wrap gap-2"><select className={selectClass} aria-label={t.hub} defaultValue={hub.id}>{state.hubs.map((item) => <option key={item.id} value={item.id}>{item.code}</option>)}</select><input className={inputClass} aria-label={t.date} type="date" defaultValue={state.generatedAt.slice(0, 10)} /><select className={selectClass} aria-label={t.status}><option>{t.allHubs}</option><option>{t.operational}</option><option>{t.degraded}</option></select></div><div className="grid gap-3 sm:grid-cols-2 lg:grid-cols-4 2xl:grid-cols-7">{metrics.map(([label, value]) => <div className="rounded-lg border bg-white p-3 shadow-line" key={label}><p className="text-[10px] font-black uppercase tracking-wider text-muted-foreground">{label}</p><p className="mt-1 text-xl font-black">{value}</p></div>)}</div></section>;
}

function Header({ eyebrow, title, subtitle, children }: { eyebrow: string; title: string; subtitle: string; children?: React.ReactNode }) { return <PageHeader eyebrow={eyebrow} title={title} subtitle={subtitle} actions={children} />; }
function Tone({ value }: { value: string }) { const tone = value === "operational" || value === "resolved" || value === "success" ? "success" : value === "critical" || value === "degraded" ? "danger" : value === "high" || value === "warning" || value === "open" ? "warning" : "info"; return <Badge tone={tone}>{value}</Badge>; }
function Kpi({ icon: Icon, label, value, detail }: { icon: typeof Activity; label: string; value: string | number; detail?: string }) { return <Panel><div className="flex items-start justify-between gap-3"><div><p className="text-xs font-black uppercase tracking-[.14em] text-muted-foreground">{label}</p><p className="mt-2 text-3xl font-black">{value}</p>{detail ? <p className="mt-1 text-xs font-bold text-muted-foreground">{detail}</p> : null}</div><Icon className="h-5 w-5 text-primary" /></div></Panel>; }
function Table({ headers, rows }: { headers: string[]; rows: React.ReactNode[][] }) { return <div className="overflow-x-auto"><table className="w-full min-w-[720px] text-left text-sm"><thead><tr className="border-b bg-muted/50">{headers.map((header) => <th className="px-3 py-2 text-xs font-black uppercase tracking-wider text-muted-foreground" key={header}>{header}</th>)}</tr></thead><tbody>{rows.map((row, index) => <tr className="border-b last:border-0" key={index}>{row.map((cell, cellIndex) => <td className="px-3 py-3 font-semibold" key={cellIndex}>{cell}</td>)}</tr>)}</tbody></table></div>; }

function ControlTower({ state, t, source, formatDate }: { state: EnterpriseHubState; t: Copy; source: string; formatDate: (value: string) => string }) {
  const totals = state.hubs.reduce((a, h) => ({ inbound: a.inbound + h.inbound, inventory: a.inventory + h.inventory, batches: a.batches + h.batches, incidents: a.incidents + h.incidents, alerts: a.alerts + h.criticalAlerts, capacity: a.capacity + h.travelerCapacityKg, reserved: a.reserved + h.reservedCapacityKg }), { inbound: 0, inventory: 0, batches: 0, incidents: 0, alerts: 0, capacity: 0, reserved: 0 });
  return <div className="space-y-5" data-testid="control-tower"><Header eyebrow={source} title={t.controlTower} subtitle={t.controlTowerSub}><Link className="rounded-md bg-primary px-4 py-2 text-sm font-black" href="/hub/control-tower?view=executive">{t.executive}</Link></Header><div className="grid gap-3 sm:grid-cols-2 xl:grid-cols-6"><Kpi icon={Boxes} label={t.inventory} value={totals.inventory} /><Kpi icon={Activity} label={t.inbound} value={totals.inbound} /><Kpi icon={BarChart3} label={t.batches} value={totals.batches} /><Kpi icon={AlertTriangle} label={t.incidents} value={totals.incidents} /><Kpi icon={Gauge} label={t.capacity} value={`${Math.round((totals.reserved / Math.max(totals.capacity, 1)) * 100)}%`} detail={`${totals.reserved}/${totals.capacity} kg`} /><Kpi icon={ShieldCheck} label={t.alerts} value={totals.alerts} /></div><div className="grid gap-4 xl:grid-cols-[.8fr_1.2fr]"><Panel title={t.networkMap}><div className="relative h-72 overflow-hidden rounded-lg bg-black p-4 text-white"><div className="hub-grid absolute inset-0 opacity-30" />{state.hubs.map((hub, index) => <Link aria-label={hub.name} className="absolute z-10 flex h-9 w-9 items-center justify-center rounded-full border-2 border-white bg-primary text-[10px] font-black shadow-glow" href={`/hub/control-tower?hub=${hub.id}`} key={hub.id} style={{ left: `${12 + (index * 15) % 78}%`, top: `${18 + (index % 3) * 27}%` }}>{hub.code.slice(0, 3)}</Link>)}<p className="absolute bottom-3 left-3 text-xs font-bold text-white/60">{t.updated}: {formatDate(state.generatedAt)}</p></div></Panel><Panel title={t.comparison}><Table headers={[t.hub, t.status, t.inbound, t.inventory, t.batches, t.incidents, t.capacity]} rows={state.hubs.map((hub) => [<span className="font-black" key={hub.id}>{hub.code}<small className="block text-muted-foreground">{hub.city}</small></span>, <Tone key="status" value={hub.status} />, hub.inbound, hub.inventory, hub.batches, hub.incidents, `${Math.round(hub.reservedCapacityKg / Math.max(hub.travelerCapacityKg, 1) * 100)}%`])} /></Panel></div></div>;
}
function Agents({ state, t }: { state: EnterpriseHubState; t: Copy }) { return <div className="space-y-5" data-testid="agent-performance"><Header eyebrow={t.operations} title={t.agents} subtitle={t.agentsSub} /><div className="grid gap-3 md:grid-cols-4"><Kpi icon={Users} label={t.agent} value={state.agents.length} /><Kpi icon={Boxes} label={t.received} value={state.agents.reduce((a, x) => a + x.received, 0)} /><Kpi icon={Gauge} label={t.sla} value={`${Math.round(state.agents.reduce((a, x) => a + x.slaPercent, 0) / Math.max(state.agents.length, 1))}%`} /><Kpi icon={BarChart3} label={t.weight} value={`${Math.round(state.agents.reduce((a, x) => a + x.processedWeightKg, 0))} kg`} /></div><Panel><Table headers={[t.agent, t.hub, t.team, t.station, t.received, t.inspections, t.movements, t.average, t.sla, t.productivity]} rows={state.agents.map((agent) => [<strong key={agent.id}>{agent.name}</strong>, agent.hubCode, agent.team, agent.station, agent.received, agent.inspections, agent.movements, `${agent.averageSeconds}s`, `${agent.slaPercent}%`, <div className="w-28" key="productivity"><div className="h-2 rounded bg-muted"><div className="h-2 rounded bg-primary" style={{ width: `${agent.productivity}%` }} /></div><small>{agent.productivity}%</small></div>])} /></Panel></div>; }
function SearchPage({ state, t }: { state: EnterpriseHubState; t: Copy }) {
  const [results, setResults] = useState<Array<{ entity_type: string; entity_id: string; label: string; subtitle: string; href: string }>>([]);
  const [pending, setPending] = useState(false);
  async function submit(event: FormEvent<HTMLFormElement>) { event.preventDefault(); setPending(true); const params = new URLSearchParams(new FormData(event.currentTarget) as never); const response = await fetch(`/api/hub/search?${params}`); const payload = await response.json() as { results?: typeof results }; setResults(payload.results ?? []); setPending(false); }
  return <div className="space-y-5" data-testid="global-search"><Header eyebrow={t.operations} title={t.search} subtitle={t.searchSub} /><Panel><form className="grid gap-3 md:grid-cols-[1fr_220px_auto]" onSubmit={submit}><input aria-label={t.search} className={inputClass} minLength={2} name="q" placeholder={t.searchPlaceholder} required /><select aria-label={t.hub} className={selectClass} name="hub"><option value="">{t.allHubs}</option>{state.hubs.map((hub) => <option key={hub.id} value={hub.id}>{hub.code}</option>)}</select><button className={submitClass} disabled={pending} type="submit"><Search className="mr-2 h-4 w-4" />{pending ? "…" : t.searchAction}</button></form><p className="mt-3 text-xs font-bold text-muted-foreground">{t.searchHelp}</p></Panel><Panel title={t.updated}><div className="grid gap-2 md:grid-cols-3">{(results.length ? results : state.incidents.slice(0, 3).map((incident) => ({ entity_type: "incident", entity_id: incident.id, label: incident.code, subtitle: incident.title, href: `/hub/incidents?focus=${incident.id}` }))).map((result) => <Link className="rounded-md border p-3 transition hover:border-primary" href={result.href} key={`${result.entity_type}-${result.entity_id}`}><strong className="block">{result.label}</strong><span className="text-sm text-muted-foreground">{result.subtitle}</span></Link>)}</div></Panel></div>;
}
function Incidents({ state, t }: { state: EnterpriseHubState; t: Copy }) { return <div className="space-y-5" data-testid="incident-center"><Header eyebrow={t.operations} title={t.incidentsTitle} subtitle={t.incidentsSub} /><Panel title={t.createIncident}><form action="/api/hub/anomalies" className="grid gap-3 md:grid-cols-4" method="post"><input className={inputClass} name="title" placeholder={t.incidentsTitle} required /><select className={selectClass} name="type"><option value="manual_review">operational</option><option value="damaged_package">damaged_package</option><option value="qr_issue">qr_issue</option><option value="storage_issue">storage_issue</option><option value="capacity_mismatch">capacity_mismatch</option></select><select className={selectClass} name="priority"><option value="medium">medium</option><option value="high">high</option><option value="urgent">urgent</option></select><button className={submitClass} type="submit">{t.createIncident}</button><input name="description" type="hidden" value="Enterprise incident center" /><input name="returnTo" type="hidden" value="/hub/incidents" /></form></Panel><Panel><Table headers={[t.code, t.hub, t.priority, t.status, t.assigned, t.opened, t.slaRemaining]} rows={state.incidents.map((incident) => [<strong key={incident.id}>{incident.code}<small className="block max-w-xs text-muted-foreground">{incident.title}</small></strong>, incident.hubCode, <Tone key="priority" value={incident.priority} />, <Tone key="status" value={incident.status} />, incident.assignedTo, new Date(incident.openedAt).toLocaleTimeString(), `${incident.slaMinutes} min`])} /></Panel></div>; }
function Alerts({ state, t, formatDate }: { state: EnterpriseHubState; t: Copy; formatDate: (value: string) => string }) { return <div className="space-y-5" data-testid="realtime-alerts"><Header eyebrow={t.operations} title={t.alertsTitle} subtitle={t.alertsSub} /><div className="grid gap-3">{state.alerts.map((alert) => <Panel key={alert.id}><div className="flex flex-col gap-3 md:flex-row md:items-center md:justify-between"><div><div className="flex gap-2"><Tone value={alert.severity} /><Tone value={alert.status} /></div><h2 className="mt-2 font-black">{alert.title}</h2><p className="text-sm text-muted-foreground">{alert.message}</p></div><div className="text-right text-xs font-bold text-muted-foreground"><p>{alert.hubCode}</p><p>{formatDate(alert.triggeredAt)}</p></div></div></Panel>)}</div></div>; }
function Stock({ state, t }: { state: EnterpriseHubState; t: Copy }) { const total = state.hubs.reduce((a, h) => a + h.inventory, 0); return <div className="space-y-5" data-testid="stock-monitoring"><Header eyebrow={t.operations} title={t.stock} subtitle={t.stockSub} /><div className="grid gap-3 md:grid-cols-5"><Kpi icon={Boxes} label={t.inventory} value={total} /><Kpi icon={Gauge} label={t.occupation} value="12.6%" /><Kpi icon={AlertTriangle} label={t.dormant} value={12} /><Kpi icon={Building2} label={t.noLocation} value={4} /><Kpi icon={ShieldCheck} label={t.discrepancy} value={2} /></div><Panel title={t.physicalCount}><Table headers={[t.hub, t.inventory, t.weight, t.occupation, t.status]} rows={state.hubs.map((hub) => [hub.code, hub.inventory, `${hub.storedWeightKg} kg`, `${Math.round(hub.storedWeightKg / Math.max(hub.storageCapacityKg, 1) * 100)}%`, <Tone key="status" value={hub.status} />])} /></Panel></div>; }
function Forecast({ state, t }: { state: EnterpriseHubState; t: Copy }) { return <div className="space-y-5" data-testid="forecast"><Header eyebrow={t.executive} title={t.forecast} subtitle={t.forecastSub} /><Panel><Table headers={[t.hub, t.date, t.expected, t.expectedWeight, t.travelerCapacity, t.agentsNeeded, t.locationsNeeded, t.saturationRisk, t.delayRisk]} rows={state.forecasts.slice(0, 21).map((item) => [item.hubCode, item.date, item.expectedPackages, `${item.expectedWeightKg} kg`, `${item.travelerCapacityKg} kg`, item.recommendedAgents, item.recommendedLocations, `${item.saturationRisk}%`, `${item.delayRisk}%`])} /></Panel></div>; }
function SystemHealth({ state, t }: { state: EnterpriseHubState; t: Copy }) { const entries = [["Continuité des opérations", state.health.database], ["Accès collaborateurs", state.health.auth], ["Documents", state.health.storage], ["Mises à jour", state.health.realtime], ["Services métier", state.health.api], ["Scanner", state.health.scanner]] as const; return <div className="space-y-5" data-testid="system-health"><Header eyebrow={t.operations} title={t.health} subtitle={t.healthSub} /><div className="grid gap-3 sm:grid-cols-2 xl:grid-cols-3">{entries.map(([service, status]) => <Panel key={service}><div className="flex items-center justify-between"><div><p className="text-xs font-black uppercase text-muted-foreground">{t.service}</p><p className="mt-1 text-xl font-black">{service}</p></div><Tone value={status} /></div></Panel>)}</div><div className="grid gap-3 md:grid-cols-3"><Kpi icon={Activity} label={t.latency} value={`${state.health.latencyMs} ms`} /><Kpi icon={Gauge} label={t.availability} value={`${state.health.availabilityPercent}%`} /><Kpi icon={ShieldCheck} label={t.version} value={state.health.version} /></div></div>; }
function Audit({ state, t, formatDate }: { state: EnterpriseHubState; t: Copy; formatDate: (value: string) => string }) { return <div className="space-y-5" data-testid="audit-log"><Header eyebrow={t.executive} title={t.audit} subtitle={t.auditSub} /><Panel><Table headers={[t.actor, t.role, t.hub, t.action, t.resource, t.result, t.risk, t.correlation, t.date]} rows={state.audit.map((event) => [event.actor, event.role, event.hubCode, event.action, event.resource, <Tone key="result" value={event.result} />, <Tone key="risk" value={event.risk} />, <code className="text-xs" key="correlation">{event.correlationId}</code>, formatDate(event.occurredAt)])} /></Panel></div>; }
function Reports({ state, t }: { state: EnterpriseHubState; t: Copy }) { const inventory = state.hubs.reduce((sum, hub) => sum + hub.inventory, 0); const weight = state.hubs.reduce((sum, hub) => sum + hub.storedWeightKg, 0); const quality = Math.round(state.agents.reduce((sum, agent) => sum + agent.slaPercent, 0) / Math.max(state.agents.length, 1)); return <div className="space-y-5" data-testid="reporting"><h2 className="sr-only">{t.legacyReports}</h2><Header eyebrow={t.executive} title={t.reports} subtitle={t.reportsSub} /><div className="grid gap-3 md:grid-cols-4"><Kpi icon={Boxes} label={t.inventory} value={inventory} /><Kpi icon={Gauge} label={t.weight} value={`${Math.round(weight)} kg`} /><Kpi icon={BarChart3} label={t.growth} value="+12.4%" /><Kpi icon={ShieldCheck} label={t.quality} value={`${quality}%`} /></div><Panel title={t.reportsTrend}><div className="flex h-52 items-end gap-3">{state.forecasts.slice(0, 12).map((item, index) => <div className="group flex flex-1 flex-col items-center gap-2" key={`${item.hubCode}-${item.date}`}><div className="w-full rounded-t bg-primary transition group-hover:bg-black" style={{ height: `${Math.max(15, item.expectedPackages / 4)}px` }} /><span className="hidden text-[10px] font-bold xl:block">{index + 1}</span></div>)}</div></Panel></div>; }
function Exports({ state, session, t }: { state: EnterpriseHubState; session: HubSession; t: Copy }) { return <div className="space-y-5" data-testid="exports"><Header eyebrow={t.executive} title={t.exports} subtitle={t.exportsSub} /><Panel><form action="/api/hub/exports" className="grid gap-3 md:grid-cols-4" method="get"><select className={selectClass} name="type"><option value="inventory">{t.inventoryExport}</option><option value="incidents">{t.incidentsExport}</option><option value="audit">{t.auditExport}</option><option value="performance">{t.performanceExport}</option></select><select className={selectClass} name="format"><option value="csv">CSV</option><option value="xlsx">Excel</option><option value="pdf">PDF</option><option value="print">{t.print}</option></select><select className={selectClass} name="hub"><option value="">{t.allHubs}</option>{state.hubs.map((hub) => <option key={hub.id} value={hub.id}>{hub.code}</option>)}</select><button className={submitClass} type="submit"><Download className="mr-2 h-4 w-4" />{t.generate}</button><input name="author" type="hidden" value={session.name} /></form></Panel></div>; }
function Documents({ t }: { t: Copy }) { const docs = [["manifest", t.manifest], ["packing-list", t.packingList], ["batch", t.batchSheet], ["inspection", t.inspectionSheet], ["inventory", t.inventorySheet]]; return <div className="space-y-5" data-testid="documents"><Header eyebrow={t.operations} title={t.documents} subtitle={t.documentsSub} /><div className="grid gap-3 md:grid-cols-2 xl:grid-cols-3">{docs.map(([type, label]) => <Panel key={type}><FileText className="h-7 w-7 text-primary" /><h2 className="mt-3 text-lg font-black">{label}</h2><Link className="mt-4 inline-flex rounded-md bg-black px-4 py-2 text-sm font-black text-white" href={`/api/hub/exports?type=${type}&format=print`}>{t.print}</Link></Panel>)}</div></div>; }
