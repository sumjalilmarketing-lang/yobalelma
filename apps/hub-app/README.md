# Yobalelma Hub App

Target domain: `hub.yobalelma.com`

Roles:

- `hub_agent`
- `hub_manager`

Migration status: planned. Existing Hub routes remain in the root Next.js app until the dedicated app receives its own layout, middleware and deployment.
