"use client";
export default function ErrorPage({ reset }: { error: Error & { digest?: string }; reset: () => void }) { return <div className="rounded-xl border border-error/30 bg-error/10 p-8"><p className="text-xs font-black uppercase tracking-[.18em] text-error">Incident d’interface</p><h1 className="mt-2 text-3xl font-black">La vue n’a pas pu être chargée.</h1><p className="mt-2 text-sm text-muted-foreground">Vos actions terrain restent dans la file hors ligne. Vous pouvez relancer sans perte.</p><button onClick={reset} className="mt-5 rounded-lg bg-black px-4 py-2 text-sm font-black text-white">Réessayer</button></div>; }
